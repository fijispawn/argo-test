server {
    listen       80;
    server_name  _;

    root   /usr/share/nginx/html;
    index  index.html;

    # Отдача статики
    location /assets/ {
        try_files $uri =404;
    }

    # SPA роутинг: любые пути -> index.html
    location / {
        try_files $uri /index.html;
    }
}